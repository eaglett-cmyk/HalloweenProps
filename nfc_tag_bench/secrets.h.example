#pragma once
// Copy this file to "secrets.h" and fill in your WiFi credentials.
// secrets.h is gitignored, so your password never lands in the repo.
#define WIFI_SSID  "your-wifi-name"
#define WIFI_PASS  "your-wifi-password"
